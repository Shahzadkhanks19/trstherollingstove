import { getAuthenticatedUser } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { AppError } from "@/lib/errors/AppError";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { submitNps } from "@/services/feedback-reputation.service";import { npsCreateSchema } from "@/validators/feedback-reputation";
export async function POST(request:Request){try{const user=await getAuthenticatedUser();if(!user)throw new AppError("Authentication required.",401);const input=await validateRequestBody(request,npsCreateSchema);await connectToDatabase();return successResponse(await submitNps({...input,customerId:user.id}),"Feedback recorded.",201)}catch(error){return handleApiError(error)}}