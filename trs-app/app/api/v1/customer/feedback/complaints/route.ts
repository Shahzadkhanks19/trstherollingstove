import { getAuthenticatedUser } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { AppError } from "@/lib/errors/AppError";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { CustomerComplaint } from "@/models/CustomerComplaint";import { createComplaint } from "@/services/feedback-reputation.service";import { complaintCreateSchema } from "@/validators/feedback-reputation";
export async function GET(){try{const user=await getAuthenticatedUser();if(!user)throw new AppError("Authentication required.",401);await connectToDatabase();return successResponse(await CustomerComplaint.find({customerId:user.id}).sort({createdAt:-1}).lean());}catch(error){return handleApiError(error)}}
export async function POST(request:Request){try{const user=await getAuthenticatedUser();if(!user)throw new AppError("Authentication required.",401);const input=await validateRequestBody(request,complaintCreateSchema);await connectToDatabase();return successResponse(await createComplaint({...input,customerId:user.id}),"Complaint submitted.",201)}catch(error){return handleApiError(error)}}