import { requireAuthenticatedUser } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { AppError } from "@/lib/errors/AppError";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { redeemCatalogReward } from "@/services/loyalty.service";import { rewardRedeemSchema } from "@/validators/loyalty";
export async function POST(request:Request){try{const actor=await requireAuthenticatedUser();if(actor.roleKey!=="customer")throw new AppError("Customer access required.",403);const input=await validateRequestBody(request,rewardRedeemSchema);await connectToDatabase();return successResponse(await redeemCatalogReward(actor.id,input.rewardId),"Reward redeemed.",201);}catch(error){return handleApiError(error);}}
