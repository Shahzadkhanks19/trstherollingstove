import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getUpsellRecommendations } from "@/services/recommendation.service";
export async function GET(request:Request){try{await connectToDatabase();const p=new URL(request.url).searchParams;const limit=Math.min(30,Math.max(1,Number(p.get("limit")||6)));const menuItemId=p.get("menuItemId");if(!menuItemId)return successResponse([],"No menu item supplied.");return successResponse(await getUpsellRecommendations(menuItemId,limit),"Recommendations loaded.");}catch(error){return handleApiError(error);}}
