import { getAuthenticatedUser } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getPersonalizedRecommendations,getTrendingRecommendations } from "@/services/recommendation.service";
export async function GET(request:Request){try{await connectToDatabase();const user=await getAuthenticatedUser();const limit=Math.min(30,Math.max(1,Number(new URL(request.url).searchParams.get("limit")||10)));const data=user?await getPersonalizedRecommendations(user.id,limit):await getTrendingRecommendations(limit,"7d");return successResponse(data,"Recommendations loaded.");}catch(error){return handleApiError(error);}}
