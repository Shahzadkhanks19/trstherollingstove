import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getTrendingRecommendations } from "@/services/recommendation.service";
export async function GET(request:Request){try{await connectToDatabase();const p=new URL(request.url).searchParams;const limit=Math.min(30,Math.max(1,Number(p.get("limit")||10)));const window=(p.get("window")||"7d") as "24h"|"7d"|"30d"|"all_time";return successResponse(await getTrendingRecommendations(limit,window),"Trending recommendations loaded.");}catch(error){return handleApiError(error);}}
