import { getAuthenticatedUser } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { recordRecommendationEvent } from "@/services/recommendation.service";import { recommendationEventSchema } from "@/validators/recommendations";
export async function POST(request:Request){try{const user=await getAuthenticatedUser();const input=await validateRequestBody(request,recommendationEventSchema);await connectToDatabase();const event=await recordRecommendationEvent({...input,customerId:user?.id??null});return successResponse({id:event.id},"Recommendation event recorded.",201);}catch(error){return handleApiError(error);}}
