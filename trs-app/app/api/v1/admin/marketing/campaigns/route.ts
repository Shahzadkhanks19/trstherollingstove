import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {createCampaign,listCampaigns} from "@/services/marketing.service";import {campaignCreateSchema} from "@/validators/marketing";export async function GET(request:Request){try{await requirePermission("users.read");await connectToDatabase();const r=await listCampaigns(new URL(request.url).searchParams);return successResponse(r.rows,"Campaigns loaded.",200,{page:r.page,limit:r.limit,total:r.total,totalPages:r.totalPages});}catch(error){return handleApiError(error);}}export async function POST(request:Request){try{const user=await requirePermission("users.update");await connectToDatabase();const input=campaignCreateSchema.parse(await request.json());return successResponse(await createCampaign(input,user.id),"Campaign created.",201);}catch(error){return handleApiError(error);}}
