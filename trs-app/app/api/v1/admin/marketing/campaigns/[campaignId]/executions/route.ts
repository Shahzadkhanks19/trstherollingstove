import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {listExecutions} from "@/services/marketing.service";export async function GET(_:Request,{params}:{params:Promise<{campaignId:string}>}){try{await requirePermission("users.read");await connectToDatabase();const {campaignId}=await params;return successResponse(await listExecutions(campaignId),"Campaign executions loaded.");}catch(error){return handleApiError(error);}}
