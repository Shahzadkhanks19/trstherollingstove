import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {runCampaign} from "@/services/marketing.service";import {campaignRunSchema} from "@/validators/marketing";export async function POST(request:Request,{params}:{params:Promise<{campaignId:string}>}){try{await requirePermission("users.update");await connectToDatabase();const {campaignId}=await params;const input=campaignRunSchema.parse(await request.json().catch(()=>({})));return successResponse(await runCampaign(campaignId,input.dryRun),input.dryRun?"Audience preview generated.":"Campaign queued.",201);}catch(error){return handleApiError(error);}}
