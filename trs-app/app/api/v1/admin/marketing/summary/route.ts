import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {getMarketingSummary} from "@/services/marketing.service";export async function GET(){try{await requirePermission("users.read");await connectToDatabase();return successResponse(await getMarketingSummary(),"Marketing summary loaded.");}catch(error){return handleApiError(error);}}
