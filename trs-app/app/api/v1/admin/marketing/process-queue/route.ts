import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {processQueuedExecutions} from "@/services/marketing.service";export async function POST(request:Request){try{await requirePermission("users.update");await connectToDatabase();const body=await request.json().catch(()=>({}));const limit=Math.min(500,Math.max(1,Number(body.limit||100)));return successResponse(await processQueuedExecutions(limit),"Marketing queue processed.");}catch(error){return handleApiError(error);}}
