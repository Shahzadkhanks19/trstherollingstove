import { requirePermission } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { handleApiError } from "@/lib/errors/handleApiError"; import { CustomerProfile } from "@/models/CustomerProfile"; import { User } from "@/models/User"; import { getCustomerRoleId } from "@/services/userManagement.service"; import { buildCsv } from "@/utils/csv";
export async function GET() { try { await requirePermission("users.read"); await connectToDatabase(); const roleId = await getCustomerRoleId(); const users = await User.find({ roleId }).select("name email phone isActive emailVerifiedAt lastLoginAt createdAt").sort({ createdAt: -1 }).limit(10000).lean(); const profiles = await CustomerProfile.find({ userId: { $in: users.map(u => u._id) } }).lean(); const map = new Map(profiles.map(p => [String(p.userId), p])); const csv = buildCsv(["Name","Email","Phone","Status","Verified","Source","Tags","Last Login","Created At"], users.map(u => { const p = map.get(String(u._id)); return [u.name,u.email,u.phone ?? "",u.isActive ? "Active":"Inactive",u.emailVerifiedAt ? "Yes":"No",p?.source ?? "",p?.tags?.join("|") ?? "",u.lastLoginAt?.toISOString() ?? "",u.createdAt.toISOString()]; })); return new Response(csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="trs-customers-${new Date().toISOString().slice(0,10)}.csv"`}}); } catch(e){ return handleApiError(e); } }
