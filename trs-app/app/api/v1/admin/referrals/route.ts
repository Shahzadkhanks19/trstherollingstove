import "@/models/User";
import { requirePermission } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { Referral } from "@/models/Referral";
export async function GET(request:Request){try{await requirePermission("users.read");await connectToDatabase();const u=new URL(request.url),page=Math.max(Number(u.searchParams.get("page")||1),1),limit=Math.min(Math.max(Number(u.searchParams.get("limit")||25),1),100),search=u.searchParams.get("search")?.trim(),status=u.searchParams.get("status");const filter:Record<string,unknown>={};if(status&&status!=="all")filter.status=status;if(search)filter.referralCode={$regex:search,$options:"i"};const [referrals,total,stats]=await Promise.all([Referral.find(filter).populate("referrerCustomerId","name email phone").populate("referredCustomerId","name email phone").sort({createdAt:-1}).skip((page-1)*limit).limit(limit).lean(),Referral.countDocuments(filter),Referral.aggregate([{$group:{_id:"$status",count:{$sum:1}}}])]);return successResponse({referrals,pagination:{page,limit,total,pages:Math.ceil(total/limit)},stats:Object.fromEntries(stats.map(x=>[x._id,x.count]))},"Referrals loaded.");}catch(e){return handleApiError(e)}}
