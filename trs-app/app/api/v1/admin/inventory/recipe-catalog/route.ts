import { requirePermission } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { InventoryItem } from "@/models/InventoryItem"; import { MenuItem } from "@/models/MenuItem";
export const dynamic="force-dynamic";
export async function GET(){try{await requirePermission("inventory.read");await connectToDatabase();const [inventoryItems,menuItems]=await Promise.all([InventoryItem.find({isActive:true}).select("name sku category unit currentStock averageUnitCost").sort({category:1,name:1}).lean(),MenuItem.find({isActive:true,deletedAt:null}).select("name slug basePrice variants preparationTimeMinutes").sort({name:1}).lean()]);return successResponse({inventoryItems,menuItems});}catch(e){return handleApiError(e)}}
