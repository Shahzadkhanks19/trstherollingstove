import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { KitchenProductionRun } from "@/models/KitchenProductionRun";export const dynamic="force-dynamic";export async function GET(){try{await requirePermission("inventory.read");await connectToDatabase();const rows=await KitchenProductionRun.find().sort({createdAt:-1}).limit(100).lean();return successResponse(rows)}catch(e){return handleApiError(e)}}
