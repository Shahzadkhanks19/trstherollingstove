import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { ReviewRequest } from "@/models/ReviewRequest";import { scheduleReviewRequest } from "@/services/feedback-reputation.service";import { reviewRequestCreateSchema } from "@/validators/feedback-reputation";
export async function GET(){try{await requirePermission("reviews.read");await connectToDatabase();return successResponse(await ReviewRequest.find().populate("customerId","name email phone").populate("orderId","orderNumber").sort({createdAt:-1}).limit(1000).lean());}catch(error){return handleApiError(error)}}
export async function POST(request:Request){try{const actor=await requirePermission("reviews.manage");const input=await validateRequestBody(request,reviewRequestCreateSchema);await connectToDatabase();return successResponse(await scheduleReviewRequest({...input,customerId:actor.id}),"Review request scheduled.",201);}catch(error){return handleApiError(error)}}