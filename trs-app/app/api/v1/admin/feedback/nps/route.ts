import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { NpsResponse } from "@/models/NpsResponse";export async function GET(){try{await requirePermission("reviews.read");await connectToDatabase();return successResponse(await NpsResponse.find().populate("customerId","name email phone").populate("orderId","orderNumber").sort({respondedAt:-1}).limit(1000).lean());}catch(error){return handleApiError(error)}}