import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getReputationSummary } from "@/services/feedback-reputation.service";export async function GET(request:Request){try{await requirePermission("reviews.read");await connectToDatabase();const days=Math.min(365,Math.max(1,Number(new URL(request.url).searchParams.get("days")??30)));return successResponse(await getReputationSummary(days));}catch(error){return handleApiError(error)}}