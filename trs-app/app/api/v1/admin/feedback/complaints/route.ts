import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { CustomerComplaint } from "@/models/CustomerComplaint";export async function GET(request:Request){try{await requirePermission("reviews.read");await connectToDatabase();const url=new URL(request.url);const status=url.searchParams.get("status");const filter:Record<string,unknown>={};if(status&&status!=="all")filter.status=status;return successResponse(await CustomerComplaint.find(filter).populate("customerId","name email phone").populate("assignedTo","name email").sort({priority:-1,createdAt:-1}).limit(500).lean());}catch(error){return handleApiError(error)}}