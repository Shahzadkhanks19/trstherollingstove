import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {listCrmCustomers} from "@/services/crm.service";export async function GET(request:Request){try{await requirePermission("users.read");await connectToDatabase();const result=await listCrmCustomers(new URL(request.url).searchParams);return successResponse(result.rows,"CRM customers loaded.",200,{page:result.page,limit:result.limit,total:result.total,totalPages:result.totalPages});}catch(error){return handleApiError(error);}}