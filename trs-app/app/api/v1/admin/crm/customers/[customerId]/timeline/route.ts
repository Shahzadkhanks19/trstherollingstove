import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {getCustomerTimeline} from "@/services/crm.service";export async function GET(request:Request,{params}:{params:Promise<{customerId:string}>}){try{await requirePermission("users.read");await connectToDatabase();const {customerId}=await params;const limit=Math.min(100,Math.max(1,Number(new URL(request.url).searchParams.get("limit")||50)));return successResponse(await getCustomerTimeline(customerId,limit),"Customer timeline loaded.");}catch(error){return handleApiError(error);}}