import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse,errorResponse} from "@/lib/http/apiResponse";import {getCustomer360} from "@/services/crm.service";export async function GET(_:Request,{params}:{params:Promise<{customerId:string}>}){try{await requirePermission("users.read");await connectToDatabase();const {customerId}=await params;const data=await getCustomer360(customerId);return data?successResponse(data,"Customer 360 loaded."):errorResponse("Customer not found.",404);}catch(error){return handleApiError(error);}}