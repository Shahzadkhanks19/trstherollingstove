import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { recommendationReportCsv } from "@/services/recommendation.service";
export async function GET(){try{await requirePermission("reports.read");await connectToDatabase();const csv=await recommendationReportCsv();return new Response(csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="trs-recommendation-analytics-${new Date().toISOString().slice(0,10)}.csv"`}});}catch(error){return handleApiError(error);}}
