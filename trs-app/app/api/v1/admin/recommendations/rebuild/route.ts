import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { rebuildRecommendationIntelligence } from "@/services/recommendation.service";import { recommendationRebuildSchema } from "@/validators/recommendations";
export async function POST(request:Request){try{await requirePermission("settings.manage");const input=await validateRequestBody(request,recommendationRebuildSchema);await connectToDatabase();return successResponse(await rebuildRecommendationIntelligence(input),"Recommendation intelligence rebuilt.");}catch(error){return handleApiError(error);}}
