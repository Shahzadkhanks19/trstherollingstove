import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { LoyaltyTier } from "@/models/LoyaltyTier";import { seedDefaultLoyaltyConfiguration } from "@/services/loyalty.service";
export async function GET(){try{await requirePermission("reports.read");await connectToDatabase();await seedDefaultLoyaltyConfiguration();return successResponse(await LoyaltyTier.find().sort({rank:1}).lean(),"Loyalty tiers loaded.");}catch(error){return handleApiError(error);}}
