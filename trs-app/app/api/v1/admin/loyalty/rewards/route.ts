import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { RewardCatalogItem } from "@/models/RewardCatalogItem";import { writeAuditLog } from "@/services/audit.service";import { rewardCatalogCreateSchema } from "@/validators/loyalty";
export async function GET(){try{await requirePermission("reports.read");await connectToDatabase();return successResponse(await RewardCatalogItem.find().sort({createdAt:-1}).lean(),"Reward catalog loaded.");}catch(error){return handleApiError(error);}}
export async function POST(request:Request){try{const actor=await requirePermission("settings.manage");const input=await validateRequestBody(request,rewardCatalogCreateSchema);await connectToDatabase();const reward=await RewardCatalogItem.create(input);await writeAuditLog({actorUserId:actor.id,action:"loyalty.reward_created",entityType:"reward_catalog_item",entityId:reward.id,description:`Created reward ${reward.name}.`});return successResponse(reward,"Reward created.",201);}catch(error){return handleApiError(error);}}
