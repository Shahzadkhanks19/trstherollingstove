import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { AppError } from "@/lib/errors/AppError";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { RewardCatalogItem } from "@/models/RewardCatalogItem";import { writeAuditLog } from "@/services/audit.service";import { rewardCatalogUpdateSchema } from "@/validators/loyalty";
export async function PATCH(request:Request,{params}:{params:Promise<{rewardId:string}>}){try{const actor=await requirePermission("settings.manage");const input=await validateRequestBody(request,rewardCatalogUpdateSchema);await connectToDatabase();const {rewardId}=await params;const reward=await RewardCatalogItem.findByIdAndUpdate(rewardId,{$set:input},{new:true,runValidators:true});if(!reward)throw new AppError("Reward not found.",404);await writeAuditLog({actorUserId:actor.id,action:"loyalty.reward_updated",entityType:"reward_catalog_item",entityId:reward.id,description:`Updated reward ${reward.name}.`});return successResponse(reward,"Reward updated.");}catch(error){return handleApiError(error);}}
export async function DELETE(_request:Request,{params}:{params:Promise<{rewardId:string}>}){try{const actor=await requirePermission("settings.manage");await connectToDatabase();const {rewardId}=await params;const reward=await RewardCatalogItem.findByIdAndUpdate(rewardId,{$set:{isActive:false}},{new:true});if(!reward)throw new AppError("Reward not found.",404);await writeAuditLog({actorUserId:actor.id,action:"loyalty.reward_deactivated",entityType:"reward_catalog_item",entityId:reward.id,description:`Deactivated reward ${reward.name}.`});return successResponse(reward,"Reward deactivated.");}catch(error){return handleApiError(error);}}
