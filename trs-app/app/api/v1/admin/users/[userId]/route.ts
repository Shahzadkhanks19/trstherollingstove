import {requirePermission} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {AppError} from "@/lib/errors/AppError";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {validateRequestBody} from "@/lib/validation/validateRequest";import {AuthSession} from "@/models/AuthSession";import {Role} from "@/models/Role";import {User} from "@/models/User";import {updateUserSchema} from "@/validators/admin";
export async function GET(_:Request,c:{params:Promise<{userId:string}>}){try{await requirePermission("users.read");const {userId}=await c.params;await connectToDatabase();const u=await User.findById(userId).populate("roleId","key name permissionIds").lean();if(!u)throw new AppError("User not found.",404);return successResponse(u,"User loaded.")}catch(e){return handleApiError(e)}}
export async function PATCH(r:Request,c:{params:Promise<{userId:string}>}){try{const a=await requirePermission("users.update"),{userId}=await c.params,i=await validateRequestBody(r,updateUserSchema);await connectToDatabase();const u=await User.findById(userId);if(!u)throw new AppError("User not found.",404);if(i.roleId&&!await Role.exists({_id:i.roleId,isActive:true}))throw new AppError("Role unavailable.",400);if(i.isActive===false&&u.id===a.id)throw new AppError("Cannot deactivate yourself.",400);const sensitive=i.roleId!==undefined||i.isActive===false;Object.assign(u,i);if(sensitive)u.tokenVersion+=1;await u.save();if(sensitive)await AuthSession.updateMany({userId:u._id,revokedAt:null},{$set:{revokedAt:new Date(),revokeReason:"Administrative update."}});return successResponse({id:u.id,name:u.name,email:u.email,phone:u.phone,roleId:u.roleId,isActive:u.isActive},"User updated.")}catch(e){return handleApiError(e)}}
