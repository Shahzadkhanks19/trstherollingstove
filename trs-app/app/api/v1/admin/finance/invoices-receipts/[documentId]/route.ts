import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { updateFinanceDocument } from "@/services/invoice-receipt.service";import { financeDocumentUpdateSchema } from "@/validators/invoice-receipt";export async function PATCH(request:Request,{params}:{params:Promise<{documentId:string}>}){try{const actor=await requirePermission("reports.read");const{documentId}=await params;const input=await validateRequestBody(request,financeDocumentUpdateSchema);return successResponse(await updateFinanceDocument(documentId,input,actor.id),"Financial document updated.");}catch(error){return handleApiError(error);}}
