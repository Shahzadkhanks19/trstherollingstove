import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { generateFinanceDocumentPdf } from "@/services/invoice-receipt.service";export async function GET(_:Request,{params}:{params:Promise<{documentId:string}>}){try{await requirePermission("reports.read");const{documentId}=await params;const{bytes,filename}=await generateFinanceDocumentPdf(documentId);return new Response(Buffer.from(bytes),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="${filename}"`,"Cache-Control":"private, no-store"}});}catch(error){return handleApiError(error);}}
