import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { cancelFinanceDocument } from "@/services/invoice-receipt.service";import { documentActionSchema } from "@/validators/invoice-receipt";export async function POST(request:Request,{params}:{params:Promise<{documentId:string}>}){try{const actor=await requirePermission("reports.read");const{documentId}=await params;const{reason}=await validateRequestBody(request,documentActionSchema);return successResponse(await cancelFinanceDocument(documentId,reason,actor.id),"Financial document cancelled.");}catch(error){return handleApiError(error);}}
