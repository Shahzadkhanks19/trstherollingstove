import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { issueFinanceDocument } from "@/services/invoice-receipt.service";export async function POST(_:Request,{params}:{params:Promise<{documentId:string}>}){try{const actor=await requirePermission("reports.read");const{documentId}=await params;return successResponse(await issueFinanceDocument(documentId,actor.id),"Financial document issued.");}catch(error){return handleApiError(error);}}
