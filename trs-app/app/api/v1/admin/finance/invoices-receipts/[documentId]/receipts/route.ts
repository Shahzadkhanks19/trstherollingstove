import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { createReceipt } from "@/services/invoice-receipt.service";import { receiptCreateSchema } from "@/validators/invoice-receipt";export async function POST(request:Request,{params}:{params:Promise<{documentId:string}>}){try{const actor=await requirePermission("reports.read");const{documentId}=await params;const input=await validateRequestBody(request,receiptCreateSchema);return successResponse(await createReceipt(documentId,input,actor.id),"Receipt recorded.",201);}catch(error){return handleApiError(error);}}
