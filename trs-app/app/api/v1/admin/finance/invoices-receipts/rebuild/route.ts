import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { buildInvoiceReceiptSnapshot } from "@/services/invoice-receipt.service";import { invoiceReceiptRebuildSchema } from "@/validators/invoice-receipt";export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,invoiceReceiptRebuildSchema);return successResponse(await buildInvoiceReceiptSnapshot({...input,generatedBy:actor.id}),"Invoice and receipt snapshot rebuilt.");}catch(error){return handleApiError(error);}}
