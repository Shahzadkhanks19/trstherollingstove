import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { createFinanceDocument } from "@/services/invoice-receipt.service";import { financeDocumentCreateSchema } from "@/validators/invoice-receipt";export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,financeDocumentCreateSchema);return successResponse(await createFinanceDocument(input,actor.id),"Financial document created.",201);}catch(error){return handleApiError(error);}}
