import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { buildFinanceGovernanceSnapshot } from "@/services/finance-governance.service";import { governanceRebuildSchema } from "@/validators/finance-governance";
export async function POST(request:Request){try{await requirePermission("settings.manage");const input=governanceRebuildSchema.parse(await request.json());return successResponse(await buildFinanceGovernanceSnapshot(input),"Finance governance snapshot rebuilt.");}catch(error){return handleApiError(error);}}
