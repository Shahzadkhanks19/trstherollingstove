import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getFinanceGovernanceSummary } from "@/services/finance-governance.service";import { governanceQuerySchema } from "@/validators/finance-governance";
export async function GET(request:Request){try{await requirePermission("reports.read");const url=new URL(request.url);const {days}=governanceQuerySchema.parse({days:url.searchParams.get("days")??30});return successResponse(await getFinanceGovernanceSummary(days));}catch(error){return handleApiError(error);}}
