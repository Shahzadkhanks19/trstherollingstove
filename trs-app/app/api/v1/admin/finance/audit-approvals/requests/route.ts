import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { createApprovalRequest,listApprovalRequests } from "@/services/finance-governance.service";import { approvalCreateSchema } from "@/validators/finance-governance";
export async function GET(){try{await requirePermission("reports.read");return successResponse(await listApprovalRequests());}catch(error){return handleApiError(error);}}
export async function POST(request:Request){try{const user=await requirePermission("settings.manage");const input=approvalCreateSchema.parse(await request.json());return successResponse(await createApprovalRequest(input,user),"Approval request created.",201);}catch(error){return handleApiError(error);}}
