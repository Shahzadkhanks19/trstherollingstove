import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { decideApprovalRequest } from "@/services/finance-governance.service";import { approvalDecisionSchema } from "@/validators/finance-governance";
export async function POST(request:Request,{params}:{params:Promise<{approvalId:string}>}){try{const user=await requirePermission("settings.manage");const {approvalId}=await params;const input=approvalDecisionSchema.parse(await request.json());return successResponse(await decideApprovalRequest(approvalId,input,user),"Approval request updated.");}catch(error){return handleApiError(error);}}
