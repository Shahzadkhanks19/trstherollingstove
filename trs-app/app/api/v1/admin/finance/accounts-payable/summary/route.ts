import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getAccountsPayableSummary } from "@/services/accounts-payable.service";import { payableRangeSchema } from "@/validators/accounts-payable";export async function GET(request:Request){try{await requirePermission("reports.read");const url=new URL(request.url);const{days}=payableRangeSchema.parse({days:url.searchParams.get("days")??30});return successResponse(await getAccountsPayableSummary(days),"Accounts payable summary loaded.");}catch(error){return handleApiError(error);}}
