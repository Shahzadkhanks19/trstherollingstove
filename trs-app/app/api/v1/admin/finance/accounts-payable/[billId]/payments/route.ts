import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { recordPayablePayment } from "@/services/accounts-payable.service";import { payablePaymentCreateSchema } from "@/validators/accounts-payable";export async function POST(request:Request,{params}:{params:Promise<{billId:string}>}){try{const actor=await requirePermission("reports.read");const{billId}=await params;const input=await validateRequestBody(request,payablePaymentCreateSchema);return successResponse(await recordPayablePayment(billId,input,actor.id),"Payable payment recorded.",201);}catch(error){return handleApiError(error);}}
