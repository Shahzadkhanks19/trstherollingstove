import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { writeOffPayable } from "@/services/accounts-payable.service";import { payableWriteOffSchema } from "@/validators/accounts-payable";export async function POST(request:Request,{params}:{params:Promise<{billId:string}>}){try{const actor=await requirePermission("reports.read");const{billId}=await params;const input=await validateRequestBody(request,payableWriteOffSchema);return successResponse(await writeOffPayable(billId,input.amount,input.reason,actor.id),"Payable write-off recorded.");}catch(error){return handleApiError(error);}}
