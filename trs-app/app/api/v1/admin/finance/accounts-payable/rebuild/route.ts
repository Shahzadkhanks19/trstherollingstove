import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { buildAccountsPayableSnapshot } from "@/services/accounts-payable.service";import { payableRebuildSchema } from "@/validators/accounts-payable";export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,payableRebuildSchema);return successResponse(await buildAccountsPayableSnapshot({...input,generatedBy:actor.id}),"Accounts payable snapshot rebuilt.");}catch(error){return handleApiError(error);}}
