import { requirePermission } from "@/lib/auth/session";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { createPayableBill } from "@/services/accounts-payable.service";import { payableBillCreateSchema } from "@/validators/accounts-payable";export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,payableBillCreateSchema);return successResponse(await createPayableBill(input,actor.id),"Payable bill created.",201);}catch(error){return handleApiError(error);}}
