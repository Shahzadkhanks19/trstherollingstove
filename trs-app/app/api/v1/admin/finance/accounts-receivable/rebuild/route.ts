import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { buildAccountsReceivableSnapshot } from "@/services/accounts-receivable.service"; import { receivableRebuildSchema } from "@/validators/accounts-receivable"; export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,receivableRebuildSchema);return successResponse(await buildAccountsReceivableSnapshot({...input,generatedBy:actor.id}),"Accounts receivable snapshot rebuilt.");}catch(error){return handleApiError(error);}}
