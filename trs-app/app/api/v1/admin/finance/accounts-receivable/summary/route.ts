import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { getAccountsReceivableSummary } from "@/services/accounts-receivable.service"; import { receivableRangeSchema } from "@/validators/accounts-receivable"; export async function GET(request:Request){try{await requirePermission("reports.read");const url=new URL(request.url);const{days}=receivableRangeSchema.parse({days:url.searchParams.get("days")??30});return successResponse(await getAccountsReceivableSummary(days),"Accounts receivable summary loaded.");}catch(error){return handleApiError(error);}}
