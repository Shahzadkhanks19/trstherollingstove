import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { createReceivableInvoice } from "@/services/accounts-receivable.service"; import { receivableInvoiceCreateSchema } from "@/validators/accounts-receivable"; export async function POST(request:Request){try{const actor=await requirePermission("reports.read");const input=await validateRequestBody(request,receivableInvoiceCreateSchema);return successResponse(await createReceivableInvoice(input,actor.id),"Receivable invoice created.",201);}catch(error){return handleApiError(error);}}
