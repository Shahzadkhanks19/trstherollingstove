import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { recordReceivablePayment } from "@/services/accounts-receivable.service"; import { receivablePaymentCreateSchema } from "@/validators/accounts-receivable"; export async function POST(request:Request,context:{params:Promise<{invoiceId:string}>}){try{const actor=await requirePermission("reports.read");const{invoiceId}=await context.params;const input=await validateRequestBody(request,receivablePaymentCreateSchema);return successResponse(await recordReceivablePayment(invoiceId,input,actor.id),"Receivable payment recorded.",201);}catch(error){return handleApiError(error);}}
