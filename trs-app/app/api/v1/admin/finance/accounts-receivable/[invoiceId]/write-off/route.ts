import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { writeOffReceivable } from "@/services/accounts-receivable.service"; import { receivableWriteOffSchema } from "@/validators/accounts-receivable"; export async function POST(request:Request,context:{params:Promise<{invoiceId:string}>}){try{const actor=await requirePermission("reports.read");const{invoiceId}=await context.params;const input=await validateRequestBody(request,receivableWriteOffSchema);return successResponse(await writeOffReceivable(invoiceId,input.amount,input.reason,actor.id),"Receivable balance written off.");}catch(error){return handleApiError(error);}}
