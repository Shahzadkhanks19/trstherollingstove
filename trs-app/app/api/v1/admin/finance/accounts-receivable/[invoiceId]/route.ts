import { requirePermission } from "@/lib/auth/session"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { updateReceivableInvoice } from "@/services/accounts-receivable.service"; import { receivableInvoiceUpdateSchema } from "@/validators/accounts-receivable"; export async function PATCH(request:Request,context:{params:Promise<{invoiceId:string}>}){try{const actor=await requirePermission("reports.read");const{invoiceId}=await context.params;const input=await validateRequestBody(request,receivableInvoiceUpdateSchema);return successResponse(await updateReceivableInvoice(invoiceId,input,actor.id),"Receivable invoice updated.");}catch(error){return handleApiError(error);}}
