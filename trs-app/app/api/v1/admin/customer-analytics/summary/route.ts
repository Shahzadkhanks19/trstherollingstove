import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { getCustomerAnalytics,getCustomerAnalyticsHistory } from "@/services/customer-analytics.service";import { customerAnalyticsQuerySchema } from "@/validators/customer-analytics";
export async function GET(request:Request){try{await requirePermission("reports.read");await connectToDatabase();const parsed=customerAnalyticsQuerySchema.parse(Object.fromEntries(new URL(request.url).searchParams));const [snapshot,history]=await Promise.all([getCustomerAnalytics(parsed.days,parsed.cohortMonths),getCustomerAnalyticsHistory()]);return successResponse({snapshot,history});}catch(error){return handleApiError(error)}}
