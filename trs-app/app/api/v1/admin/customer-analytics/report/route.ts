import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { getCustomerAnalytics } from "@/services/customer-analytics.service";export async function GET(){await requirePermission("reports.read");await connectToDatabase();const data=await getCustomerAnalytics(365,12);const m=data.metrics as Record<string,unknown>;const rows=[["Metric","Value"],...Object.entries(m).map(([key,value])=>[key,String(value??"")])];const csv=rows.map(row=>row.map(value=>`"${String(value).replaceAll('"','""')}"`).join(",")).join("\n");return new Response(csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":"attachment; filename=trs-customer-analytics-report.csv"}})}
