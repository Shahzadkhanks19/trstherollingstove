import { requirePermission } from "@/lib/auth/session";import { connectToDatabase } from "@/lib/db/mongoose";import { handleApiError } from "@/lib/errors/handleApiError";import { successResponse } from "@/lib/http/apiResponse";import { validateRequestBody } from "@/lib/validation/validateRequest";import { buildCustomerAnalytics } from "@/services/customer-analytics.service";import { customerAnalyticsRebuildSchema } from "@/validators/customer-analytics";
export async function POST(request:Request){try{await requirePermission("reports.read");const input=await validateRequestBody(request,customerAnalyticsRebuildSchema);await connectToDatabase();return successResponse(await buildCustomerAnalytics(input),"Customer analytics rebuilt.");}catch(error){return handleApiError(error)}}
