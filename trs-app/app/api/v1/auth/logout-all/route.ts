import {clearAuthCookies} from "@/lib/auth/cookies";import {requireAuthenticatedUser} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {AuthSession} from "@/models/AuthSession";import {User} from "@/models/User";
export async function POST(){try{const u=await requireAuthenticatedUser();await connectToDatabase();await Promise.all([AuthSession.updateMany({userId:u.id,revokedAt:null},{$set:{revokedAt:new Date(),revokeReason:"Logout all."}}),User.findByIdAndUpdate(u.id,{$inc:{tokenVersion:1}})]);await clearAuthCookies();return successResponse(null,"Logged out from all devices.")}catch(e){return handleApiError(e)}}
