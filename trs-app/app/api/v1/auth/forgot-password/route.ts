import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {validateRequestBody} from "@/lib/validation/validateRequest";import {issuePasswordReset} from "@/services/auth.service";import {forgotPasswordSchema} from "@/validators/auth";
export async function POST(r:Request){try{const i=await validateRequestBody(r,forgotPasswordSchema);await connectToDatabase();await issuePasswordReset(i.email);return successResponse(null,"If an eligible account exists, instructions were sent.")}catch(e){return handleApiError(e)}}
