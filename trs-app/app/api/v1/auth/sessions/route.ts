import {requireAuthenticatedUser} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {AuthSession} from "@/models/AuthSession";
export async function GET(){try{const u=await requireAuthenticatedUser();await connectToDatabase();const rows=await AuthSession.find({userId:u.id,revokedAt:null,expiresAt:{$gt:new Date()}}).select("userAgent ipAddress lastUsedAt expiresAt createdAt").sort({lastUsedAt:-1}).lean();return successResponse(rows.map(x=>({...x,isCurrent:String(x._id)===u.sessionId})),"Sessions loaded.")}catch(e){return handleApiError(e)}}
