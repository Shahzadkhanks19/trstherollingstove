import {requireAuthenticatedUser} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {AppError} from "@/lib/errors/AppError";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {AuthSession} from "@/models/AuthSession";
export async function DELETE(_:Request,c:{params:Promise<{sessionId:string}>}){try{const u=await requireAuthenticatedUser(),{sessionId}=await c.params;await connectToDatabase();const s=await AuthSession.findOneAndUpdate({_id:sessionId,userId:u.id,revokedAt:null},{$set:{revokedAt:new Date(),revokeReason:"Revoked by user."}});if(!s)throw new AppError("Session not found.",404);return successResponse(null,"Session revoked.")}catch(e){return handleApiError(e)}}
