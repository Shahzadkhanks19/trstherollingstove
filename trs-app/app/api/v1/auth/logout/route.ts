import {clearAuthCookies} from "@/lib/auth/cookies";import {getAuthenticatedUser} from "@/lib/auth/session";import {connectToDatabase} from "@/lib/db/mongoose";import {handleApiError} from "@/lib/errors/handleApiError";import {successResponse} from "@/lib/http/apiResponse";import {AuthSession} from "@/models/AuthSession";
export async function POST(){try{const u=await getAuthenticatedUser();if(u){await connectToDatabase();await AuthSession.findByIdAndUpdate(u.sessionId,{$set:{revokedAt:new Date(),revokeReason:"User logout."}})}await clearAuthCookies();return successResponse(null,"Logout successful.")}catch(e){await clearAuthCookies();return handleApiError(e)}}
