import { requirePermission } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { settleRunningOrder } from "@/services/pos-operations.service"; import { settleRunningOrderSchema } from "@/validators/pos-operations";
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { try { const actor = await requirePermission("pos.use"); const input = await validateRequestBody(request, settleRunningOrderSchema); const { id } = await context.params; await connectToDatabase(); return successResponse(await settleRunningOrder(id, input, actor.id), "Running order settled."); } catch (error) { return handleApiError(error); } }
