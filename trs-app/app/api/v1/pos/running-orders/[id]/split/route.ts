import { requirePermission } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { splitRunningOrder } from "@/services/pos-operations.service"; import { splitRunningOrderSchema } from "@/validators/pos-operations";
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { try { const actor = await requirePermission("pos.use"); const input = await validateRequestBody(request, splitRunningOrderSchema); const { id } = await context.params; await connectToDatabase(); return successResponse(await splitRunningOrder(id, input.lineQuantities, input.targetTableId, actor.id), "Order split."); } catch (error) { return handleApiError(error); } }
