import { requireAuthenticatedUser } from "@/lib/auth/session"; import { connectToDatabase } from "@/lib/db/mongoose"; import { AppError } from "@/lib/errors/AppError"; import { handleApiError } from "@/lib/errors/handleApiError"; import { successResponse } from "@/lib/http/apiResponse"; import { validateRequestBody } from "@/lib/validation/validateRequest"; import { StaffProfile } from "@/models/StaffProfile"; import { User } from "@/models/User"; import { serializeStaff } from "@/services/userManagement.service"; import { staffSelfUpdateSchema } from "@/validators/userManagement";
async function auth(){const u=await requireAuthenticatedUser();if(u.roleKey==="customer")throw new AppError("Staff access required.",403);return u;}
export async function GET(){try{const u=await auth();await connectToDatabase();return successResponse(await serializeStaff(u.id),"Profile loaded.");}catch(e){return handleApiError(e);}}
export async function PATCH(request:Request){try{const a=await auth();const input=await validateRequestBody(request,staffSelfUpdateSchema);await connectToDatabase();const user=await User.findById(a.id);if(!user)throw new AppError("Account not found.",404);if(input.name!==undefined)user.name=input.name;if(input.phone!==undefined)user.phone=input.phone??undefined;if(input.avatarUrl!==undefined)user.avatarUrl=input.avatarUrl;await user.save();if(input.profile){const p=await StaffProfile.findOne({userId:user._id});if(!p)throw new AppError("Staff profile not found.",404);Object.assign(p,input.profile);await p.save();}return successResponse(await serializeStaff(user.id),"Profile updated.");}catch(e){return handleApiError(e);}}
