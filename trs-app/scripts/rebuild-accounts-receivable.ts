import { buildAccountsReceivableSnapshot } from "@/services/accounts-receivable.service"; async function main(){const days=Math.min(366,Math.max(1,Number(process.argv[2]??30)));const snapshot=await buildAccountsReceivableSnapshot({days,source:"scheduled"});console.log(JSON.stringify({ok:true,days,periodKey:snapshot?.periodKey,generatedAt:snapshot?.generatedAt},null,2));process.exit(0);}main().catch(error=>{console.error(error);process.exit(1);});
