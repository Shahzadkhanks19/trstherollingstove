import { connectToDatabase } from "../lib/db/mongoose";import { buildCustomerAnalytics } from "../services/customer-analytics.service";async function main(){await connectToDatabase();const result=await buildCustomerAnalytics({days:365,cohortMonths:12,source:"script"});console.log({snapshotId:result.id,generatedAt:result.generatedAt});}main().then(()=>process.exit(0)).catch(error=>{console.error(error);process.exit(1)});
