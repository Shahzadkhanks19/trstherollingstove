import { connectToDatabase } from "../lib/db/mongoose";import { User } from "../models/User";import { rebuildCustomerRecommendationCache } from "../services/recommendation.service";import { getCustomerRoleId } from "../services/userManagement.service";
async function main(){await connectToDatabase();const roleId=await getCustomerRoleId();const limit=Number(process.env.RECOMMENDATION_CUSTOMER_LIMIT||10000);const customers=await User.find({roleId,isActive:true}).select("_id").limit(limit).lean();let processed=0;for(const customer of customers){await rebuildCustomerRecommendationCache(String(customer._id));processed++;}console.log({processed,generatedAt:new Date()});process.exit(0);}main().catch(error=>{console.error(error);process.exit(1);});
