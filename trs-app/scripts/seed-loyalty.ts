import { connectToDatabase } from "@/lib/db/mongoose";import { LoyaltyMilestone } from "@/models/LoyaltyMilestone";import { seedDefaultLoyaltyConfiguration } from "@/services/loyalty.service";
async function main(){await connectToDatabase();const tiers=await seedDefaultLoyaltyConfiguration();const milestones=[{key:"first-5-orders",name:"First 5 Orders",description:"Complete five TRS orders.",metric:"annual_orders",threshold:5,rewardCoins:50,sortOrder:1},{key:"spend-10000",name:"₹10,000 Club",description:"Reach ₹10,000 lifetime spend.",metric:"lifetime_spend",threshold:10000,rewardCoins:150,sortOrder:2},{key:"spend-25000",name:"TRS VIP",description:"Reach ₹25,000 lifetime spend.",metric:"lifetime_spend",threshold:25000,rewardCoins:300,sortOrder:3}];for(const m of milestones)await LoyaltyMilestone.updateOne({key:m.key},{$setOnInsert:{...m,isActive:true}},{upsert:true});console.log(`Seeded ${tiers.length} tiers and ${milestones.length} milestones.`);}
main().then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)});
