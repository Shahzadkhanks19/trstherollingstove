"use client";import{useEffect,useState}from"react";import Link from"next/link";type Data={order:{orderNumber:string;status:string;paymentStatus:string;grandTotal:number};events:Array<{at:string;type:string;title:string;detail:string;actor:string}>};type Api<T>={success:boolean;message:string;data:T};export function OrderTimelineClient({orderId}:{orderId:string}){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");useEffect(()=>{let active=true;void fetch(`/api/v1/pos/orders/${orderId}/timeline`,{cache:"no-store"}).then(async r=>{const j=await r.json() as Api<Data>;if(!r.ok)throw new Error(j.message);if(active)setData(j.data)}).catch(e=>active&&setError(e instanceof Error?e.message:"Unable to load timeline."));return()=>{active=false}},[orderId]);return <div className="space-y-5"><Link href="/admin/orders" className="text-sm font-black text-[#C8102E]">← Back to orders</Link><header><p className="text-[10px] font-black uppercase tracking-[.22em] text-[#C8102E]">Order audit</p><h1 className="mt-2 text-4xl font-black text-[#173044]">{data?.order.orderNumber??"Order timeline"}</h1>{data?<p className="mt-2 text-sm text-slate-500">{data.order.status} · {data.order.paymentStatus} · ₹{data.order.grandTotal}</p>:null}</header>{error?<p className="rounded-xl bg-red-50 p-4 font-bold text-red-700">{error}</p>:null}<section className="rounded-3xl border bg-white p-5"><div className="space-y-0">{data?.events.map((event,index)=><div key={`${event.at}-${index}`} className="relative grid grid-cols-[24px_1fr] gap-4 pb-6 last:pb-0"><div className="relative"><span className="absolute left-2 top-2 h-full w-px bg-slate-200 last:hidden"/><span className="relative block h-4 w-4 rounded-full bg-[#C8102E] ring-4 ring-red-50"/></div><div><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-black text-[#173044]">{event.title}</h2><time className="text-xs font-bold text-slate-500">{new Date(event.at).toLocaleString("en-IN")}</time></div>{event.detail?<p className="mt-1 text-sm text-slate-600">{event.detail}</p>:null}{event.actor?<p className="mt-1 text-xs font-bold text-slate-400">By {event.actor}</p>:null}</div></div>)}</div></section></div>}
