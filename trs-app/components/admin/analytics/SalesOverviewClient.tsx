"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Area, AreaChart, Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis } from "recharts";
type Data={summary:{netSales:number;orders:number;averageOrderValue:number};trend:Array<{period:string;netSales:number;orders:number}>;payments:Array<{method:string;amount:number}>};
const money=new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0});
export function SalesOverviewClient(){const [data,setData]=useState<Data|null>(null);useEffect(()=>{const id=window.setTimeout(async()=>{const today=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());const r=await fetch(`/api/v1/admin/sales-analytics?from=${today}&to=${today}`,{cache:"no-store"});if(r.ok){const j=await r.json();setData(j.data)}},0);return()=>window.clearTimeout(id)},[]);if(!data)return null;return <section className="mt-5 rounded-3xl border bg-white p-5"><div className="flex items-start justify-between"><div><h2 className="text-xl font-black text-[#173044]">Sales overview</h2><p className="text-sm text-slate-500">Today’s revenue, order flow and collection mix.</p></div><Link href="/admin/sales-analytics" className="text-xs font-black text-[#C8102E]">Full analytics →</Link></div><div className="mt-5 grid gap-4 lg:grid-cols-3"><div className="h-52 min-w-0"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data.trend}><XAxis dataKey="period" hide/><Tooltip formatter={v=>money.format(Number(v))}/><Area type="monotone" dataKey="netSales" stroke="#C8102E" fill="#C8102E" fillOpacity={.16}/></AreaChart></ResponsiveContainer></div><div className="h-52 min-w-0"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.trend}><XAxis dataKey="period" hide/><Tooltip/><Bar dataKey="orders" fill="#173044" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div><div className="h-52 min-w-0"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data.payments} dataKey="amount" nameKey="method" innerRadius={45} outerRadius={72}>{data.payments.map((_,i)=><Cell key={i} fill={["#C8102E","#173044","#E8A53A","#64748B"][i%4]}/>)}</Pie><Tooltip formatter={v=>money.format(Number(v))}/></PieChart></ResponsiveContainer></div></div><div className="grid grid-cols-3 gap-3"><Mini label="Net sales" value={money.format(data.summary.netSales)}/><Mini label="Orders" value={String(data.summary.orders)}/><Mini label="Average" value={money.format(data.summary.averageOrderValue)}/></div></section>}
function Mini({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-[#fff8f3] p-3"><p className="text-lg font-black text-[#173044]">{value}</p><p className="text-[9px] font-black uppercase tracking-wider text-slate-500">{label}</p></div>}
