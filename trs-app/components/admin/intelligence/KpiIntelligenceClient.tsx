"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRotateRight, faDownload, faTriangleExclamation } from "@fortawesome/free-solid-svg-icons";
import { PageHeader, SectionCard } from "@/components/admin/AdminPrimitives";
import type { KpiIntelligenceResult } from "@/services/kpi-intelligence.service";
const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
function Metric({label,value,change,detail}:{label:string;value:string;change?:number;detail:string}){return <article className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase tracking-widest text-slate-400">{label}</p><p className="mt-3 text-3xl font-black text-[#173044]">{value}</p>{change!==undefined?<p className={`mt-1 text-xs font-black ${change>=0?"text-emerald-700":"text-red-600"}`}>{change>=0?"+":""}{change}% vs previous</p>:null}<p className="mt-2 text-xs font-bold text-slate-500">{detail}</p></article>}
export function KpiIntelligenceClient(){const [lookbackDays,setLookbackDays]=useState(30);const [report,setReport]=useState<KpiIntelligenceResult|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const query=useMemo(()=>new URLSearchParams({lookbackDays:String(lookbackDays)}).toString(),[lookbackDays]);const load=useCallback(async(refresh=false)=>{setLoading(true);setError("");try{const response=await fetch(`/api/v1/admin/kpi-intelligence?${query}${refresh?"&refresh=true":""}`,{cache:"no-store"});const payload=await response.json();if(!response.ok)throw new Error(payload.message||"Unable to load KPI intelligence.");setReport(payload.data);}catch(cause){setError(cause instanceof Error?cause.message:"Unable to load KPI intelligence.");}finally{setLoading(false);}},[query]);useEffect(()=>{const timer=window.setTimeout(()=>{void load();},0);return()=>window.clearTimeout(timer);},[load]);const maxRevenue=Math.max(0,...(report?.daily.map((row)=>row.revenue)??[]));return <div className="space-y-6"><PageHeader eyebrow="Phase 4.2.5 · Batch 2" title="Advanced KPI Intelligence" description="Comparable-period growth, rolling performance, cost pressure and automatically prioritised executive exceptions." action={<div className="flex flex-wrap gap-2">{(["csv","xlsx","pdf"] as const).map((format)=><a key={format} href={`/api/v1/admin/kpi-intelligence?${query}&format=${format}`} className="rounded-xl border bg-white px-4 py-2 text-xs font-black uppercase"><FontAwesomeIcon icon={faDownload} className="mr-2"/>{format}</a>)}<button onClick={()=>void load(true)} disabled={loading} className="rounded-xl bg-[#173044] px-4 py-2 text-xs font-black text-white disabled:opacity-50"><FontAwesomeIcon icon={faArrowRotateRight} className="mr-2"/>Refresh</button></div>}/><SectionCard title="Analysis period" subtitle="Current period is compared with the immediately preceding period of equal length."><div className="flex flex-wrap gap-2">{[14,30,60,90,180].map((days)=><button key={days} onClick={()=>setLookbackDays(days)} className={`rounded-full border px-4 py-2 text-xs font-black ${lookbackDays===days?"border-[#C8102E] bg-[#C8102E] text-white":"bg-white"}`}>{days} days</button>)}</div></SectionCard>{error?<p className="rounded-xl bg-red-50 p-4 font-bold text-red-700">{error}</p>:null}{loading?<SectionCard title="Loading intelligence"><p className="py-12 text-center font-bold text-slate-400">Calculating KPIs…</p></SectionCard>:null}{report&&!loading?<><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Revenue" value={money(report.kpis.revenue)} change={report.kpis.revenueChange} detail={`${money(report.kpis.rolling7Revenue)} in the latest 7 days`}/><Metric label="Customer orders" value={String(report.kpis.orders)} change={report.kpis.ordersChange} detail={`${report.kpis.uniqueCustomers} tracked customers`}/><Metric label="Average order value" value={money(report.kpis.averageOrderValue)} change={report.kpis.averageOrderValueChange} detail={`${report.kpis.discountPercent}% commercial discount rate`}/><Metric label="Food cost" value={`${report.kpis.foodCostPercent}%`} detail={`${money(report.kpis.foodCost)} tracked ingredient cost`}/></div><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><SectionCard title="Daily revenue intelligence" subtitle="Revenue movement across the selected current period."><div className="flex h-64 min-w-[650px] items-end gap-2 overflow-x-auto">{report.daily.map((row)=><div key={row.date} className="flex min-w-5 flex-1 flex-col items-center justify-end"><div title={`${row.date}: ${money(row.revenue)}`} className="w-full max-w-8 rounded-t bg-[#C8102E]" style={{height:`${maxRevenue?Math.max(3,row.revenue/maxRevenue*190):0}px`}}/><span className="mt-2 text-[9px] font-bold text-slate-400">{row.date.slice(5)}</span></div>)}</div></SectionCard><SectionCard title="Executive summary"><div className="space-y-3">{report.executiveSummary.map((item)=><p key={item} className="rounded-xl bg-slate-50 p-3 text-sm font-bold text-slate-700">{item}</p>)}</div></SectionCard></div><div className="grid gap-6 xl:grid-cols-2"><SectionCard title="Business exceptions" subtitle="Prioritised anomalies and suggested corrective actions."><div className="space-y-3">{report.alerts.map((alert)=><article key={alert.code} className="rounded-2xl border border-amber-200 bg-amber-50 p-4"><p className="font-black text-[#173044]"><FontAwesomeIcon icon={faTriangleExclamation} className="mr-2 text-amber-600"/>{alert.title}</p><p className="mt-2 text-sm text-slate-700">{alert.message}</p><p className="mt-2 text-xs font-bold text-amber-800">Action: {alert.suggestedAction}</p></article>)}{!report.alerts.length?<p className="py-8 text-center font-bold text-emerald-700">No major exceptions detected.</p>:null}</div></SectionCard><SectionCard title="Weekday performance"><div className="space-y-3">{[...report.weekdayPerformance].sort((a,b)=>b.revenue-a.revenue).map((row)=><div key={row.weekday} className="flex items-center justify-between rounded-xl border p-3"><div><p className="font-black">{row.weekday}</p><p className="text-xs text-slate-500">{row.orders} orders · AOV {money(row.averageOrderValue)}</p></div><p className="font-black text-[#C8102E]">{money(row.revenue)}</p></div>)}</div></SectionCard></div></>:null}</div>}
