"use client";
import { financeApi } from "@/lib/api/finance-client";
import { useCallback, useEffect, useState } from "react";
import Link from "next/link";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRotateRight, faChartLine, faDownload, faMoneyBillTrendUp, faScaleBalanced } from "@fortawesome/free-solid-svg-icons";
import { PageHeader, StatCard } from "@/components/admin/AdminPrimitives";

type Row = { key: string; label: string; amount: number; debit: number; credit: number };
type Snapshot = { metrics: { netRevenue:number; operatingExpenses:number; netProfit:number; profitMargin:number; netCashFlow:number; accountsReceivable:number; accountsPayable:number; netTaxPayable:number; workingCapital:number }; profitAndLoss:Row[]; cashFlow:Row[]; balanceSheet:Row[]; trialBalance:Row[]; gstSummary:Row[] };

const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
function Statement({title,rows}:{title:string;rows:Row[]}){return <section className="rounded-2xl border bg-white p-5"><h2 className="text-lg font-black">{title}</h2><div className="mt-4 space-y-2">{rows.map((item)=><div key={item.key} className="flex items-center justify-between gap-4 rounded-xl border p-3"><span className="text-sm font-bold">{item.label}</span><span className="font-black">{money(item.amount)}</span></div>)}</div></section>}
export function FinancialReportsClient(){const[days,setDays]=useState(30);const[data,setData]=useState<Snapshot|null>(null);const[error,setError]=useState("");const[working,setWorking]=useState(false);const load=useCallback(async()=>{try{setError("");setData(await financeApi<Snapshot>(`/api/v1/admin/finance/financial-reports/summary?days=${days}`));}catch(value){setError(value instanceof Error?value.message:"Unable to load financial reports.");}},[days]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);async function rebuild(){setWorking(true);try{await financeApi("/api/v1/admin/finance/financial-reports/rebuild",{method:"POST",body:JSON.stringify({days,source:"manual"})});await load();}catch(value){setError(value instanceof Error?value.message:"Rebuild failed.");}finally{setWorking(false);}}const m=data?.metrics;return <><PageHeader eyebrow="Phase 8 · Financial Management" title="Financial Reports" description="Unified P&L, cash flow, balance-sheet view, trial balance and GST-ready finance summaries." action={<div className="flex flex-wrap gap-2"><select value={days} onChange={(event)=>setDays(Number(event.target.value))} className="rounded-xl border bg-white px-4 py-3 text-xs font-black"><option value={7}>7 days</option><option value={30}>30 days</option><option value={90}>90 days</option><option value={365}>365 days</option></select><Link href={`/api/v1/admin/finance/financial-reports/report?days=${days}`} prefetch={false} className="inline-flex items-center gap-2 rounded-xl border bg-white px-4 py-3 text-xs font-black"><FontAwesomeIcon icon={faDownload}/>Export CSV</Link><button disabled={working} onClick={()=>void rebuild()} className="inline-flex items-center gap-2 rounded-xl bg-[#173044] px-4 py-3 text-xs font-black text-white"><FontAwesomeIcon icon={faArrowRotateRight}/>{working?"Rebuilding…":"Rebuild"}</button></div>}/>{error?<p className="mt-4 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-700">{error}</p>:null}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Net revenue" value={money(m?.netRevenue??0)} icon={faMoneyBillTrendUp} detail={`${m?.profitMargin??0}% profit margin`}/><StatCard label="Net profit" value={money(m?.netProfit??0)} icon={faChartLine} detail={`${money(m?.operatingExpenses??0)} operating expenses`}/><StatCard label="Net cash flow" value={money(m?.netCashFlow??0)} icon={faScaleBalanced} detail={`${money(m?.workingCapital??0)} working capital`}/><StatCard label="Net GST payable" value={money(m?.netTaxPayable??0)} icon={faScaleBalanced} detail={`AR ${money(m?.accountsReceivable??0)} · AP ${money(m?.accountsPayable??0)}`}/></div><div className="mt-6 grid gap-6 xl:grid-cols-2">{data?<><Statement title="Profit & loss" rows={data.profitAndLoss}/><Statement title="Cash flow" rows={data.cashFlow}/><Statement title="Balance-sheet view" rows={data.balanceSheet}/><Statement title="GST summary" rows={data.gstSummary}/><div className="xl:col-span-2"><Statement title="Trial balance" rows={data.trialBalance}/></div></>:null}</div></>}
