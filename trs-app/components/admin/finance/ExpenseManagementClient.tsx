"use client";
import { financeApi } from "@/lib/api/finance-client";
import { useCallback, useEffect, useState } from "react";
import Link from "next/link";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRotateRight, faDownload, faFileInvoiceDollar, faMoneyBillTransfer, faReceipt, faTriangleExclamation } from "@fortawesome/free-solid-svg-icons";
import { PageHeader, StatCard } from "@/components/admin/AdminPrimitives";

type Breakdown={key:string;count:number;total:number;paid:number;outstanding:number;tax:number};
type Expense={_id:string;expenseNumber:string;expenseDate:string;title:string;category:string;department:string;totalAmount:number;paidAmount:number;outstandingAmount:number;approvalStatus:string;paymentStatus:string};
type Summary={snapshot:{metrics:{expenseCount:number;approvedExpenses:number;totalExpenses:number;paidExpenses:number;outstandingExpenses:number;taxPaid:number;recurringCommitments:number;averageExpense:number};byCategory:Breakdown[];byDepartment:Breakdown[]};recentExpenses:Expense[]};

const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
export function ExpenseManagementClient(){const[days,setDays]=useState(30);const[data,setData]=useState<Summary|null>(null);const[error,setError]=useState("");const[working,setWorking]=useState(false);const load=useCallback(async()=>{try{setError("");setData(await financeApi<Summary>(`/api/v1/admin/finance/expenses/summary?days=${days}`));}catch(value){setError(value instanceof Error?value.message:"Unable to load expenses.");}},[days]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);async function rebuild(){setWorking(true);try{await financeApi("/api/v1/admin/finance/expenses/rebuild",{method:"POST",body:JSON.stringify({days,source:"manual"})});await load();}catch(value){setError(value instanceof Error?value.message:"Rebuild failed.");}finally{setWorking(false);}}const metrics=data?.snapshot.metrics;return <><PageHeader eyebrow="Phase 8 · Financial Management" title="Expense Management" description="Capture, approve and analyze operating, payroll, utility, vendor, marketing, maintenance and miscellaneous expenses." action={<div className="flex flex-wrap gap-2"><select value={days} onChange={event=>setDays(Number(event.target.value))} className="rounded-xl border bg-white px-4 py-3 text-xs font-black"><option value={7}>7 days</option><option value={30}>30 days</option><option value={90}>90 days</option><option value={365}>365 days</option></select><Link href={`/api/v1/admin/finance/expenses/report?days=${days}`} prefetch={false} className="inline-flex items-center gap-2 rounded-xl border bg-white px-4 py-3 text-xs font-black"><FontAwesomeIcon icon={faDownload}/>Export CSV</Link><button disabled={working} onClick={()=>void rebuild()} className="inline-flex items-center gap-2 rounded-xl bg-[#173044] px-4 py-3 text-xs font-black text-white"><FontAwesomeIcon icon={faArrowRotateRight}/>{working?"Rebuilding…":"Rebuild"}</button></div>}/>{error?<p className="mt-4 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-700">{error}</p>:null}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Total expenses" value={money(metrics?.totalExpenses??0)} icon={faReceipt} detail={`${metrics?.expenseCount??0} expense records`}/><StatCard label="Paid" value={money(metrics?.paidExpenses??0)} icon={faMoneyBillTransfer} detail="Cash outflow recorded"/><StatCard label="Outstanding" value={money(metrics?.outstandingExpenses??0)} icon={faTriangleExclamation} detail={`${money(metrics?.recurringCommitments??0)} recurring commitments`}/><StatCard label="Average expense" value={money(metrics?.averageExpense??0)} icon={faFileInvoiceDollar} detail={`${money(metrics?.taxPaid??0)} tax component`}/></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border bg-white p-5"><h2 className="text-lg font-black">Expense categories</h2><div className="mt-4 space-y-3">{data?.snapshot.byCategory.map(row=><div key={row.key} className="flex items-center justify-between rounded-xl bg-slate-50 p-3"><div><p className="font-black capitalize">{row.key.replaceAll("_"," ")}</p><p className="text-xs text-slate-500">{row.count} records · {money(row.outstanding)} outstanding</p></div><p className="font-black">{money(row.total)}</p></div>)}</div></section><section className="rounded-2xl border bg-white p-5"><h2 className="text-lg font-black">Recent expenses</h2><div className="mt-4 space-y-3">{data?.recentExpenses.map(expense=><div key={expense._id} className="rounded-xl border p-3"><div className="flex items-start justify-between gap-4"><div><p className="font-black">{expense.title}</p><p className="text-xs text-slate-500">{expense.expenseNumber} · {expense.category.replaceAll("_"," ")} · {new Date(expense.expenseDate).toLocaleDateString()}</p></div><p className="font-black">{money(expense.totalAmount)}</p></div><div className="mt-2 flex gap-2 text-[11px] font-bold uppercase"><span className="rounded-full bg-slate-100 px-2 py-1">{expense.approvalStatus}</span><span className="rounded-full bg-slate-100 px-2 py-1">{expense.paymentStatus}</span></div></div>)}</div></section></div></>}
