"use client";
import { financeApi } from "@/lib/api/finance-client";
import {useCallback,useEffect,useState} from "react";import Link from "next/link";import {FontAwesomeIcon} from "@fortawesome/react-fontawesome";import {faArrowRotateRight,faDownload,faFileShield,faScaleBalanced,faTriangleExclamation} from "@fortawesome/free-solid-svg-icons";import {PageHeader,StatCard} from "@/components/admin/AdminPrimitives";
type Data={metrics:{totalAuditEvents:number;pendingApprovals:number;approvedRequests:number;rejectedRequests:number;expiredRequests:number;criticalPending:number;pendingValue:number;averageDecisionHours:number;approvalRate:number};byModule:Array<{module:string;auditEvents:number;approvals:number;pending:number}>;recentApprovals:Array<{_id:string;requestNumber:string;title:string;module:string;status:string;priority:string;amount:number;requestedByName:string;createdAt:string}>;recentAuditEvents:Array<{_id:string;module:string;action:string;entityType:string;actorName:string;occurredAt:string}>};
const money=(v:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(v);
export function FinanceGovernanceClient(){const[days,setDays]=useState(30),[data,setData]=useState<Data|null>(null),[error,setError]=useState(""),[working,setWorking]=useState(false);const load=useCallback(async()=>{try{setError("");setData(await financeApi<Data>(`/api/v1/admin/finance/audit-approvals/summary?days=${days}`));}catch(e){setError(e instanceof Error?e.message:"Unable to load finance governance.");}},[days]);useEffect(()=>{const t=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(t);},[load]);async function rebuild(){setWorking(true);try{await financeApi("/api/v1/admin/finance/audit-approvals/rebuild",{method:"POST",body:JSON.stringify({days,source:"manual"})});await load();}catch(e){setError(e instanceof Error?e.message:"Rebuild failed.");}finally{setWorking(false);}}const m=data?.metrics;return <><PageHeader eyebrow="Phase 8 · Financial Management" title="Audit Logs & Approval Workflows" description="Immutable finance trails, maker-checker controls, approval queues, escalation visibility and governance analytics." action={<div className="flex flex-wrap gap-2"><select value={days} onChange={e=>setDays(Number(e.target.value))} className="rounded-xl border bg-white px-4 py-3 text-xs font-black"><option value={7}>7 days</option><option value={30}>30 days</option><option value={90}>90 days</option><option value={365}>365 days</option></select><Link href={`/api/v1/admin/finance/audit-approvals/report?days=${days}`} prefetch={false} className="inline-flex items-center gap-2 rounded-xl border bg-white px-4 py-3 text-xs font-black"><FontAwesomeIcon icon={faDownload}/>Export CSV</Link><button disabled={working} onClick={()=>void rebuild()} className="inline-flex items-center gap-2 rounded-xl bg-[#173044] px-4 py-3 text-xs font-black text-white"><FontAwesomeIcon icon={faArrowRotateRight}/>{working?"Rebuilding…":"Rebuild"}</button></div>}/>{error?<p className="mt-4 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-700">{error}</p>:null}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Audit events" value={String(m?.totalAuditEvents??0)} icon={faFileShield} detail={`${days}-day activity trail`}/><StatCard label="Pending approvals" value={String(m?.pendingApprovals??0)} icon={faScaleBalanced} detail={`${m?.criticalPending??0} critical`}/><StatCard label="Pending value" value={money(m?.pendingValue??0)} icon={faTriangleExclamation} detail={`${m?.averageDecisionHours??0}h average decision`}/><StatCard label="Approval rate" value={`${m?.approvalRate??0}%`} icon={faScaleBalanced} detail={`${m?.approvedRequests??0} approved · ${m?.rejectedRequests??0} rejected`}/></div><section className="mt-6 grid gap-4 lg:grid-cols-2"><div className="overflow-hidden rounded-2xl border bg-white"><div className="border-b p-5"><h2 className="text-lg font-black">Approval queue</h2></div><div className="divide-y">{(data?.recentApprovals??[]).map(r=><div key={r._id} className="p-4"><div className="flex justify-between gap-3"><div><p className="font-black">{r.title}</p><p className="text-xs text-slate-500">{r.requestNumber} · {r.module} · {r.requestedByName}</p></div><span className="text-xs font-black uppercase">{r.status}</span></div><p className="mt-2 text-sm">{money(r.amount)} · {r.priority}</p></div>)}</div></div><div className="overflow-hidden rounded-2xl border bg-white"><div className="border-b p-5"><h2 className="text-lg font-black">Recent audit trail</h2></div><div className="divide-y">{(data?.recentAuditEvents??[]).map(e=><div key={e._id} className="p-4"><p className="font-black">{e.action}</p><p className="text-xs text-slate-500">{e.module} · {e.entityType} · {e.actorName}</p></div>)}</div></div></section><section className="mt-6 overflow-hidden rounded-2xl border bg-white"><div className="border-b p-5"><h2 className="text-lg font-black">Governance by module</h2></div><div className="overflow-x-auto"><table className="min-w-[640px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-3">Module</th><th className="p-3">Audit events</th><th className="p-3">Approvals</th><th className="p-3">Pending</th></tr></thead><tbody>{(data?.byModule??[]).map(r=><tr key={r.module} className="border-t"><td className="p-3 font-black">{r.module}</td><td className="p-3">{r.auditEvents}</td><td className="p-3">{r.approvals}</td><td className="p-3">{r.pending}</td></tr>)}</tbody></table></div></section></>}
