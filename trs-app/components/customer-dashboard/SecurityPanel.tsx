"use client";
import { FormEvent, useEffect, useState } from "react";
type Session = { _id: string; userAgent: string; ipAddress: string; lastUsedAt: string; expiresAt: string; isCurrent: boolean };
type SessionsResponse = { data: Session[] };
export function SecurityPanel(){const [sessions,setSessions]=useState<Session[]>([]);const [message,setMessage]=useState("");const [saving,setSaving]=useState(false);useEffect(()=>{void fetch("/api/v1/auth/sessions",{cache:"no-store"}).then(r=>r.json()).then((b:SessionsResponse)=>setSessions(b.data??[]));},[]);async function changePassword(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setMessage("");const f=new FormData(e.currentTarget);const response=await fetch("/api/v1/auth/change-password",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword:f.get("currentPassword"),newPassword:f.get("newPassword"),confirmPassword:f.get("confirmPassword")})});const body=await response.json() as {message:string};setMessage(body.message);setSaving(false);if(response.ok)window.location.href="/login";}const input="mt-2 w-full rounded-xl border border-black/10 px-4 py-3 outline-none focus:border-[#C8102E] focus:ring-4 focus:ring-[#C8102E]/10";return <div className="grid gap-6 xl:grid-cols-2"><form onSubmit={changePassword} className="rounded-3xl border border-black/5 bg-white p-6 shadow-sm"><h1 className="text-2xl font-black">Change password</h1><p className="mt-1 text-sm text-black/50">Changing your password signs you out on every device.</p><div className="mt-6 space-y-4"><label className="block text-sm font-bold">Current password<input type="password" name="currentPassword" required className={input}/></label><label className="block text-sm font-bold">New password<input type="password" name="newPassword" required className={input}/></label><label className="block text-sm font-bold">Confirm new password<input type="password" name="confirmPassword" required className={input}/></label></div><button disabled={saving} className="mt-6 rounded-xl bg-[#C8102E] px-6 py-3 font-extrabold text-white disabled:opacity-60">{saving?"Updating…":"Update password"}</button>{message?<p className="mt-4 text-sm font-bold text-black/60">{message}</p>:null}</form><section className="rounded-3xl border border-black/5 bg-white p-6 shadow-sm"><h2 className="text-2xl font-black">Active sessions</h2><p className="mt-1 text-sm text-black/50">Devices currently signed into your TRS account.</p><div className="mt-6 divide-y divide-black/5">{sessions.map(s=><div key={s._id} className="py-4"><div className="flex items-center justify-between gap-3"><p className="font-black">{s.isCurrent?"This device":"Signed-in device"}</p>{s.isCurrent?<span className="rounded-full bg-green-100 px-3 py-1 text-xs font-extrabold text-green-700">Current</span>:null}</div><p className="mt-1 line-clamp-2 text-sm text-black/50">{s.userAgent||"Unknown browser"}</p><p className="mt-1 text-xs text-black/40">Last used {new Date(s.lastUsedAt).toLocaleString("en-IN")}</p></div>)}</div></section></div>}
