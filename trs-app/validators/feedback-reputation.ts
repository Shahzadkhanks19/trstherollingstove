import { z } from "zod";
const objectId=z.string().regex(/^[a-f\d]{24}$/i,"Invalid ID.");
export const complaintCreateSchema=z.object({orderId:objectId.nullable().optional(),category:z.enum(["food_quality","service","delay","billing","packaging","staff","hygiene","other"]),priority:z.enum(["low","medium","high","critical"]).default("medium"),subject:z.string().trim().min(3).max(160),description:z.string().trim().min(10).max(3000),source:z.enum(["web","admin","email","whatsapp","phone","review"]).default("web")});
export const complaintUpdateSchema=z.object({status:z.enum(["open","acknowledged","investigating","resolved","closed","reopened"]).optional(),priority:z.enum(["low","medium","high","critical"]).optional(),assignedTo:objectId.nullable().optional(),resolution:z.object({summary:z.string().trim().min(3).max(2000),compensationType:z.enum(["none","refund","coupon","coins","replacement"]).default("none"),compensationValue:z.number().min(0).default(0)}).optional()});
export const npsCreateSchema=z.object({orderId:objectId.nullable().optional(),score:z.number().int().min(0).max(10),comment:z.string().trim().max(1500).default(""),source:z.enum(["web","email","whatsapp","sms","admin"]).default("web"),requestId:objectId.nullable().optional()});
export const reviewRequestCreateSchema=z.object({orderId:objectId,channel:z.enum(["email","whatsapp","sms","push"]),scheduledFor:z.coerce.date().optional()});
export const integrationUpdateSchema=z.object({isEnabled:z.boolean(),locationId:z.string().trim().max(200).default(""),accountId:z.string().trim().max(200).default(""),reviewUrl:z.string().trim().url().or(z.literal("")),settings:z.record(z.string(),z.unknown()).default({})});