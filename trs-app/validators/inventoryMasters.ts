import { z } from "zod";
const objectId = z.string().regex(/^[a-f\d]{24}$/i, "Invalid ID.");
const common = { isActive: z.boolean().default(true) };
export const inventoryCategorySchema = z.object({ name: z.string().trim().min(2).max(100), code: z.string().trim().min(2).max(30).regex(/^[A-Za-z0-9_-]+$/), description: z.string().trim().max(500).default(""), sortOrder: z.number().int().min(0).default(0), ...common });
export const inventoryUnitSchema = z.object({ name: z.string().trim().min(1).max(60), symbol: z.string().trim().min(1).max(20).regex(/^[A-Za-z0-9_-]+$/), unitType: z.enum(["weight", "volume", "count", "packaging"]), baseUnitSymbol: z.string().trim().max(20).default(""), conversionFactor: z.number().positive().default(1), decimalPrecision: z.number().int().min(0).max(6).default(3), ...common });
export const inventoryBrandSchema = z.object({ name: z.string().trim().min(2).max(100), description: z.string().trim().max(500).default(""), ...common });
export const warehouseSchema = z.object({ name: z.string().trim().min(2).max(120), code: z.string().trim().min(2).max(30).regex(/^[A-Za-z0-9_-]+$/), addressLine1: z.string().trim().max(180).default(""), addressLine2: z.string().trim().max(180).default(""), city: z.string().trim().max(80).default(""), state: z.string().trim().max(80).default(""), postalCode: z.string().trim().max(20).default(""), contactName: z.string().trim().max(100).default(""), contactPhone: z.string().trim().max(20).default(""), isDefault: z.boolean().default(false), ...common });
export const storageLocationSchema = z.object({ warehouseId: objectId, name: z.string().trim().min(2).max(100), code: z.string().trim().min(1).max(30).regex(/^[A-Za-z0-9_-]+$/), locationType: z.enum(["ambient", "chilled", "frozen", "dry", "packaging", "other"]).default("ambient"), temperatureMin: z.number().nullable().default(null), temperatureMax: z.number().nullable().default(null), ...common }).refine((value) => value.temperatureMin === null || value.temperatureMax === null || value.temperatureMin <= value.temperatureMax, { message: "Minimum temperature cannot exceed maximum temperature.", path: ["temperatureMax"] });
