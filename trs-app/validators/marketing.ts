import {z} from "zod";
const channel=z.enum(["email","whatsapp","sms","push"]);
export const campaignCreateSchema=z.object({name:z.string().trim().min(2).max(120),description:z.string().trim().max(500).default(""),channel,subject:z.string().trim().max(180).default(""),message:z.string().trim().min(2).max(5000),audience:z.object({type:z.enum(["all","segment","risk","manual"]).default("all"),segmentKeys:z.array(z.string().trim().min(1).max(40)).max(20).default([]),riskLevels:z.array(z.enum(["low","medium","high"])).max(3).default([]),customerIds:z.array(z.string().regex(/^[a-f\d]{24}$/i)).max(5000).default([])}),sendAt:z.coerce.date().nullable().default(null),timezone:z.string().trim().min(1).max(80).default("Asia/Kolkata")});
export const campaignUpdateSchema=campaignCreateSchema.partial();
export const campaignRunSchema=z.object({dryRun:z.boolean().default(false)});
export const automationCreateSchema=z.object({name:z.string().trim().min(2).max(120),description:z.string().trim().max(500).default(""),trigger:z.enum(["customer_registered","birthday","anniversary","order_completed","inactive_30_days","inactive_60_days","inactive_90_days","segment_entered"]),channel,subject:z.string().trim().max(180).default(""),message:z.string().trim().min(2).max(5000),segmentKey:z.string().trim().max(40).default(""),delayMinutes:z.number().int().min(0).max(525600).default(0),isActive:z.boolean().default(true)});
export const automationUpdateSchema=automationCreateSchema.partial();
export const audienceCreateSchema=z.object({name:z.string().trim().min(2).max(120),description:z.string().trim().max(500).default(""),segmentKeys:z.array(z.string().trim().min(1).max(40)).max(20).default([]),riskLevels:z.array(z.enum(["low","medium","high"])).max(3).default([]),minimumLifetimeValue:z.number().min(0).default(0),minimumOrders:z.number().int().min(0).default(0),maximumDaysSinceLastOrder:z.number().int().min(0).nullable().default(null),isActive:z.boolean().default(true)});
